// Generated by CoffeeScript 1.4.0
(function() {
  var Character;

  chrome.app.Models.Character = Character = (function() {

    function Character(packet) {
      this.name = packet.name();
      if (packet.characterId != null) {
        this.id = packet.characterId();
      }
      if (packet.x != null) {
        this.x = packet.x();
      }
      if (packet.y != null) {
        this.y = packet.y();
      }
      this.sprite = new PIXI.Sprite(this.texture());
      this.sprite.anchor.x = this.sprite.anchor.y = 0.5;
      this.sprite.speedX = this.sprite.speedY = 2;
      this.sprite.setInteractive(true);
      this.sprite.character = this;
    }

    Character.prototype.texture = function() {
      return PIXI.Texture.fromImage("assets/textures/bunny.png");
    };

    Character.prototype.update = function(packet) {
      if (packet.x != null) {
        this.x = packet.x();
      }
      if (packet.y != null) {
        return this.y = packet.y();
      }
    };

    return Character;

  })();

}).call(this);
